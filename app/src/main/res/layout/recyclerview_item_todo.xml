<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="85dp"
    android:descendantFocusability="blocksDescendants"
    android:clickable="true"
    android:focusable="true"
    android:focusableInTouchMode="true"
    android:background="@drawable/recycler_background">

    <TextView
        android:id="@+id/todo_name"
        android:layout_width="fill_parent"
        android:layout_height="50dp"
        android:gravity="center_vertical"
        android:paddingLeft="12dp"
        android:textColor="#ff000000"
        android:textSize="20dp"
        android:visibility="visible"
        android:layout_below="@+id/todo_undo_button"
        android:layout_toEndOf="@+id/todo_user_name" />

    <Button
        android:id="@+id/todo_button"
        style="@style/Widget.AppCompat.Button.Colored"
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentTop="true"
        android:layout_marginEnd="8dp"
        android:textAppearance="@style/TextAppearance.AppCompat.Body2"
        android:textColor="#ffffffff"
        android:visibility="visible" />

    <TextView
        android:id="@+id/todo_user_name"
        android:layout_width="fill_parent"
        android:layout_height="35dp"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/todo_name"
        android:gravity="center_vertical"
        android:paddingLeft="12dp"
        android:textColor="#ff000000"
        android:visibility="gone" />

    <Button
        android:id="@+id/todo_undo_button"
        style="@style/Widget.AppCompat.Button.Small"
        android:layout_width="wrap_content"
        android:layout_height="35dp"
        android:layout_alignEnd="@+id/todo_button"
        android:layout_alignParentBottom="true"
        android:gravity="top"
        android:text="Undo"
        android:textAppearance="@style/TextAppearance.AppCompat.Body1"
        android:textColor="#ff000000"
        android:visibility="gone" />

</RelativeLayout>
